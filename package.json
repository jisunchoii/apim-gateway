{
  "name": "apim-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check scripts/keycloak/keycloak-device-auth.js && node --check scripts/keycloak/terraform-settings.js && node --check scripts/keycloak/keycloak-token.js && node --check scripts/keycloak/opencode-keycloak-hook.js"
  },
  "engines": {
    "node": ">=20"
  }
}
